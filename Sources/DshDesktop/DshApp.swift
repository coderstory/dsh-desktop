import SwiftUI

@main
struct DshApp: App {
    var body: some Scene {
        Window("dsh", id: "main") {
            Text("dsh — placeholder")
                .frame(width: 400, height: 200)
        }
    }
}